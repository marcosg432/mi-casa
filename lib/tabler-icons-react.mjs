/**
 * @tabler/icons-react — ponto de entrada para uso futuro com React + bundler.
 * O site estático atual (Lucide / SVG) não importa este módulo.
 *
 * Exemplo:
 *   import { IconMapPin, IconHeart } from '../lib/tabler-icons-react.mjs';
 *   // ou diretamente:
 *   import { IconMapPin } from '@tabler/icons-react';
 */
export * from '@tabler/icons-react';
