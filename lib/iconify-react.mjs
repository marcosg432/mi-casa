/**
 * @iconify/react — ponto de entrada para uso futuro com React + bundler.
 * O site estático atual (Lucide / SVG) não importa este módulo.
 *
 * Exemplo:
 *   import { Icon } from '../lib/iconify-react.mjs';
 *   // ou: import { Icon } from '@iconify/react';
 *   <Icon icon="mdi:home" width="24" height="24" />
 *
 * Catálogo: https://icon-sets.iconify.design/
 */
export { Icon, InlineIcon, iconLoaded, loadIcons, loadIcon, buildIcon, getIcon, addIcon, addCollection, listIcons, enableCache, disableCache, replaceIDs } from '@iconify/react';
